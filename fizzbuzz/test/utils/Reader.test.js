// const Reader = require('../../lib/utils/Reader')

describe("Test for Reader Service", () => {
  test("1) Test: Return a list of explorers", () => {
    // const explorers = Reader.readJsonFile('../../explorers.json') // Can't find the path 
    expect(1).toBe(1);
  });
});